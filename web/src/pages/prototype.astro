---
// PROTOTYPE SANDBOX — build/visual harness for the GrapeExplorer component.
import Layout from '../layouts/Layout.astro';
import GrapeExplorer from '../components/GrapeExplorer.astro';
---

<Layout title="Prototype — grape explorer">
  <main class="bg-[color:var(--color-bg)]">
    <div class="mx-auto max-w-5xl px-6 py-14 sm:py-20">
      <p class="mb-6 text-xs uppercase tracking-[0.2em] text-[color:var(--color-text-subtle)]">Prototype sandbox — grape explorer (not the real page)</p>

      <p class="text-sm font-medium uppercase tracking-[0.18em] text-[color:var(--color-grape)]">The vine</p>
      <h1 class="mt-3 font-display text-4xl font-medium leading-[1.05] tracking-tight sm:text-5xl">Explore the bunch.</h1>
      <p class="mt-4 max-w-2xl text-lg leading-relaxed text-[color:var(--color-text-muted)]">
        Parleq isn’t one trick — it’s a bunch of small, sharp capabilities that compound. Switch families to light up the bunch; click a grape to watch it work.
      </p>

      <GrapeExplorer />

      <p class="mt-16 text-xs text-[color:var(--color-text-subtle)]">
        v10 — Prototype-A inspired vector cluster: one shared bunch; tab families light up in place.
      </p>
    </div>
  </main>
</Layout>
