import XCTest
@testable import ParleqAppCore

final class SmokeTests: XCTestCase {
    func test_module_imports_successfully() {
        // Trivial body — the existence of @testable import and a
        // passing test proves the test target builds against the
        // library target. Real tests follow in later tasks.
        XCTAssertTrue(true)
    }
}
