Parleq 0.31.0

Smarter on-device cleanup: the Lightweight tier now fixes to/two/too homophone mistakes (like "delegate two subagents" → "delegate to subagents") using surrounding-word evidence and the recognizer's own confidence — conservatively, so real counts like "two questions" are never touched.
